spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/autohub_order_db
  rabbitmq:
    host: rabbitmq
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka